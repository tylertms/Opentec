[dependency-groups]
build = [
    "meson",
    "ninja",
]
quality = [
    "clang-tidy==22.1.8",
    "pre-commit==4.6.0",
]

[tool.uv]
default-groups = ["build", "quality"]

[tool.ruff]
target-version = "py39"
