[dependency-groups]
build = [
    "meson",
    "ninja",
]
quality = [
    "clang-tidy",
    "pre-commit",
]

[tool.uv]
default-groups = [
    "build",
    "quality"
]

[tool.ruff]
target-version = "py39"
